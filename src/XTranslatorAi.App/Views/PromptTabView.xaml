<UserControl x:Class="XTranslatorAi.App.Views.PromptTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Margin="0,10,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Base prompt (embedded, per franchise):" FontWeight="SemiBold" />
        <TextBox Grid.Row="1"
                 Text="{Binding BasePromptText, Mode=OneWay}"
                 IsReadOnly="True"
                 Style="{StaticResource XT.EditorTextBox}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" />

        <WrapPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,6" VerticalAlignment="Center">
            <CheckBox IsChecked="{Binding UseCustomPrompt}" Content="Use custom prompt (optional)" Margin="0,0,16,0" />
            <CheckBox IsChecked="{Binding UseRecStyleHints}" Content="REC 스타일 힌트(추천)" Margin="0,0,16,0" />
        </WrapPanel>
        <Border Grid.Row="3"
                Margin="0,0,0,8"
                BorderBrush="{StaticResource XT.BorderBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8">
            <Border.Style>
                <Style TargetType="{x:Type Border}">
                    <Setter Property="Background" Value="{StaticResource XT.Row.DoneBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasPromptLintIssues}" Value="True">
                            <Setter Property="Background" Value="{StaticResource XT.Row.InProgressBrush}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HasPromptLintBlockingIssues}" Value="True">
                            <Setter Property="Background" Value="{StaticResource XT.Row.ErrorBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel>
                <TextBlock Text="{Binding PromptLintSummary}" FontWeight="SemiBold" />
                <TextBlock Text="{Binding PromptLintDetails}" Margin="0,4,0,0" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
        <TextBox Grid.Row="4"
                 Text="{Binding CustomPromptText}"
                 Style="{StaticResource XT.EditorTextBox}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto" />
    </Grid>
</UserControl>
