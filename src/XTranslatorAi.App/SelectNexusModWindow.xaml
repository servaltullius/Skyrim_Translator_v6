<Window x:Class="XTranslatorAi.App.SelectNexusModWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Nexus Mod"
        Height="520"
        Width="980"
        MinHeight="420"
        MinWidth="760"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="ModsGrid"
                  Grid.Row="0"
                  Style="{StaticResource XT.DataGrid}"
                  ColumnHeaderStyle="{StaticResource XT.DataGridColumnHeader}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  AlternationCount="2"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ModId" Binding="{Binding ModId}" Width="90" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="260" />
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="100" />
                <DataGridTextColumn Header="Domain" Binding="{Binding DomainName}" Width="160" />
                <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <DockPanel Grid.Row="1" Margin="0,10,0,0" LastChildFill="False">
            <TextBlock DockPanel.Dock="Left" VerticalAlignment="Center" Text="Select the correct mod, then click OK." />
            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                <Button Content="OK" Width="90" Margin="6,0,0,0" Click="Ok_Click" />
                <Button Content="Cancel" Width="90" Margin="6,0,0,0" Click="Cancel_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>

